spring:
  datasource:
    username: pet_parks
    password: pet_parks
    url: jdbc:mysql://DC1:3306/pet_parks
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true 
    
  sql:
    init:
      mode: never  